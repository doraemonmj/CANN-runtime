# .readthedocs.yaml
version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"

python:
  install:
    - requirements: requirements.txt

# Sphinx 配置
sphinx:
  configuration: docs_sphinx/source/conf.py  # ← 指向你的 conf.py 路径
 